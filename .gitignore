# Installed tooling (local-only, ~1GB) - reinstall via its own setup script
.claude/skills/gstack/

# Dependencies & build artifacts
node_modules/
.next/
.open-next/
.wrangler/
test-results/
playwright-report/
.lighthouseci/
*.tsbuildinfo
package-lock.json

# Environment & secrets
.env
.env.*
!.env.example

# Local diagnostic output (browser-automation traces, ad-hoc Lighthouse HTML,
# build logs). These are throwaway artifacts from investigating the performance
# and contact gates; none of them belong in history.
.playwright-mcp/
*.report.html
.next-build.log

# OS / editor noise
Thumbs.db
.DS_Store
.vscode/

